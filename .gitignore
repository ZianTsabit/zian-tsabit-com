# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.idea
.venv
__pycache__
.env
minio_data/

# Postgres is now the only backend; a stray local sqlite file would be dead
# weight, not a fallback.
ziantsabit-be/db.sqlite3

# collectstatic's output. Inside the container it goes to /srv/static
# (DJANGO_STATIC_ROOT); a bare `manage.py collectstatic` outside Docker lands
# here instead. Build artefact either way -- never committed.
ziantsabit-be/staticfiles/
