Django>=4.2
# DRF 3.15 imports Django's removed ``cc_delim_re`` helper and cannot start
# against Django 5.2. DRF 3.16 adds Django 5.2 support.
djangorestframework>=3.16
drf-spectacular>=0.27
django-cors-headers>=4.4
# psycopg3's binary wheel bundles libpq, so no system Postgres client headers
# are needed to install this.
psycopg[binary]>=3.2
# Uploaded images go to an S3-compatible bucket (RustFS locally). The `s3` extra
# pulls boto3; the backend is the same one a real S3/R2 deployment would use, so
# changing the server is an environment change rather than a code change --
# swapping MinIO for RustFS touched only compose and settings, no Python.
django-storages[s3]>=1.14
# Required by DRF's ImageField, which is what actually verifies an upload is an
# image rather than a renamed archive with an image Content-Type.
Pillow>=10.0
# Production WSGI server. `manage.py runserver` is single-threaded and refuses
# to serve static files with DEBUG=0; docker-compose.prod.yml overrides CMD to
# run this instead.
gunicorn>=23.0
# Serves Django admin's and DRF's own static files from the app process. With
# DEBUG=0 nothing else does -- there is no nginx in front of the API, only
# cloudflared -- so without this /admin/ renders unstyled.
whitenoise>=6.6
