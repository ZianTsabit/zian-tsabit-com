.git
.gitignore
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# The dev database is not image content. Compose bind-mounts the source tree so
# local runs still use it; a container without that mount gets a fresh one from
# the migrate in entrypoint.sh.
db.sqlite3

Dockerfile
docker-compose.yml
